#include <stdio.h>
#include <unistd.h>

// the goal of this script is to understand how IO redirection works.
// we need to fork the process, use open(), and then dup2 to redirect.

int main() {
    
}